import styled from "styled-components";

export const InputSearchContainer = styled.div`
  position: relative;

  input {
    padding-right: 40px;
  }

  svg {
    color: #2873b6;
    position: absolute;
    right: 16px;
    top: 8px;
  }
`;
